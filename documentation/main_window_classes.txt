@startuml

class MainWindow #green{
    __init__()
    _init_menu()
    _init_toolbar()
    _init_status_line()
    _init_canvas()
    _init_control_frame()
    _init_layers_selector()
    _bind_controllers()
    load_interferogram_dialog()
    save_phases_dialog()
}

MainWindow o-down[#olive]- MainMenu
MainWindow o-down[#olive]- Toolbar
MainWindow o-down[#olive]- StatusLine
MainWindow o-down[#olive]- ScalableCanvas
MainWindow o-down[#olive]- ControlFrame
MainWindow o-down[#olive]- LayersSelector

class MainMenu #blue
class Toolbar # blue
class StatusLine #blue {
    set(format_string, *args)
    clear()
}

class ScalableCanvas #yellow {
    scale_image(percent)
    crawl(dx, dy)
    get_coords(x, y)
}
note "Holds canvas for layers. \n Can change scale of image \n and crawl across it."

class ControlFrame #yellow {
}
note "Configures and starts \n calculations."

class LayersSelector #yellow {
}

@enduml